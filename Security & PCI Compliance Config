// config/security.config.js
module.exports = {
    pci: {
        level: 'PCI-DSS-Level-1',
        encryption: {
            algorithm: 'aes-256-gcm',
            keyRotationDays: 30,
            masterKeySource: 'HSM' // Hardware Security Module
        },
        tokenization: {
            enabled: true,
            provider: 'internal',
            tokenFormat: 'tok_{random}'
        },
        dataRetention: {
            fullCardData: 'NEVER',
            last4: 'INDEFINITE',
            tokenOnly: true
        }
    },
    
    fraud: {
        rules: {
            maxAmount: 500000, // $5,000 per transaction
            velocityCheck: {
                windowMs: 3600000, // 1 hour
                maxAttempts: 5
            },
            geoBlocking: {
                enabled: true,
                blockedCountries: ['KP', 'IR', 'SY']
            },
            binCheck: {
                enabled: true,
                rejectPrepaid: false
            }
        },
        scoring: {
            lowRisk: 0.3,
            mediumRisk: 0.6,
            highRisk: 0.8,
            autoBlock: 0.9
        }
    },
    
    encryption: {
        cardNumber: {
            storage: 'TOKENIZED',
            transmission: 'TLS-1.3',
            display: 'LAST_4_ONLY'
        },
        cvv: {
            storage: 'NEVER',
            transmission: 'ENCRYPTED',
            validation: 'IMMEDIATE'
        }
    }
};
